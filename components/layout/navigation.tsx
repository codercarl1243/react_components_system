import SkipLink from "@/components/skiplink";

export default function Navigation() {

    return (
        <header>
            <SkipLink />
            <nav>
                nav
            </nav>
        </header>
    )
}
import SkipLink from "../skiplink";

export default function Navigation() {

    return (
        <header>
            <SkipLink />
            <nav>
                nav
            </nav>
        </header>
    )
}
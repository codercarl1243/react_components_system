import AnchorHeading from "@/components/heading/anchorHeading";
import PostSection from "@/components/post/post.section";

export default function section3() {
    return (
        <PostSection id="global-tokens-theming">
            <AnchorHeading as={"h2"} prefix="Step 1 —" id="global-tokens-theming-heading">
                Global design tokens
            </AnchorHeading>
            {/* Define color tokens (with naming strategy) */}
            {/* Mention other token types (spacing, typography) */}
            {/* Link to GitHub for full set */}
        </PostSection>
    );
}
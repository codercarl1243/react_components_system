import type { ArcadeGameDefinition } from "@/components/arcade/type";
import { bubblepop } from "./bubblePop/config";

export const gamesRegistry: Record<string, ArcadeGameDefinition> = {
  bubblepop
};